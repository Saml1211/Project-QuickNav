# Example .env file for note_append.py
# Copy to .env and edit values as needed.
#
# Any variable can be set here and will be loaded automatically on script start if python-dotenv is installed.
# CLI arguments override .env values.
#
# Best practices:
# - NEVER commit your real .env file with API keys to version control.
# - Use this template for sharing configuration requirements.

# OpenAI API key (required for --ai mode)
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY

# Local LLM endpoint and model (used with --llm mode)
LLM_ENDPOINT=http://localhost:11434/api/generate
LLM_MODEL=llama3

# You can set other environment variables as needed.